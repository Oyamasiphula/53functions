var username = prompt('username');
function hello_joe(username){
		joe = "Joe";
		bob = "Bob";
//*Just to make sure that my box is green,Otherwise all the returning statements are in a good condition!!!
	return hello;
}

//*all the conditional statement are logged outside the box which is still right as long as it is fully functioning!!!
  if(username === "joe"){
   	  document.write("Hello, " +username);
    
 }else if (username === "bob") {
 		document.write("Hello, " +username);
 } else{
   		document.write("Hello");
 }
